#include "Headers.h"

int valida_atributos_reserva(Reserva *p_reserva);

int valida_intersecao_datas(); //TODO: Necessario fazer uma busca pelo passageiro para checar caso haja intersecao

int valida_string_nulo(char *string);

int valida_tamanho_string(char *string, int tamanho, char *comparador);

int valida_data(Data *p_data);
